import { list } from "./input"
import Array from "array"

let mut increases = 0
let checkIncreases = (curr, index) => {
  if (index > 0 && curr > list[index - 1]) {
    increases +=1
  }
}
Array.forEachi(checkIncreases, list)

print(increases)
